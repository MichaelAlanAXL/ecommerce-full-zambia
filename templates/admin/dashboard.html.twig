{% extends "admin/layout.html.twig" %}

{% block title %}Dashboard - Admin{% endblock %}

{% block content %}
    <h2>Dashboard</h2>
    <p>Resumo rápido do sistema:</p>
    <ul>
        <li>Total de categorias: {{ totalCategorias|default(0) }}</li>
        <li>Total de produtos: {{ totalProdutos|default(0) }}</li>
        <li>Total de pedidos: {{ totalPedidos|default(0) }}</li>
    </ul>
{% endblock %}
