{% extends "admin/layout.html.twig" %}

{% block title %}Dashboard - Admin{% endblock %}

{% block content %}
    <h2>Dashboard</h2>
    <p>Resumo rápido do sistema:</p>
    <ul>
        <li>Total de categorias: {{ totalCategories | default(0) }}</li>
        <li>Total de produtos: {{ totalProducts | default(0) }}</li>
        <li>Total de pedidos: {{ totalOrders | default(0) }}</li>
    </ul>
{% endblock %}
