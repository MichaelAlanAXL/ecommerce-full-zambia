<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Admin{% endblock %}</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <style>
        body {
            display: flex;
            min-height: 100vh;
        }
        .sidebar {
            width: 220px;
            background: #343a40;
            color: #fff;
            padding: 1rem;
        }
        .sidebar a {
            color: #ddd;
            display: block;
            margin: 0.5rem 0;
            text-decoration: none;
        }
        .sidebar a:hover {
            color: #fff;
        }
        .content {
            flex: 1;
            padding: 1rem;
        }
        header {
            background: #f8f9fa;
            padding: 0.5rem 1rem;
            border-bottom: 1px solid #ddd;
        }
    </style>
</head>
<body>
    <div class="sidebar">
        <h4>Admin</h4>
        <a href="/admin">Dashboard</a>
        <a href="/admin/categorias">Categorias</a>
        <a href="/admin/produtos">Produtos</a>
        <a href="/admin/pedidos">Pedidos</a>

        <hr>
        <h6>Categorias</h6>
        <ul>
            {% for cat in categories %}
                <li><a href="#">{{ cat.descategory }}</a></li>
            {% else %}
                <li><em>Nenhuma categoria</em></li>
            {% endfor %}
        </ul>
    </div>
    <div class="content">
        <header>
            <h5>Bem-vindo, {{ username|default('Admin') }}</h5>
        </header>
        <main>
            {% block content %}{% endblock %}
        </main>
        <footer class="mt-4 text-center text-muted">
            <small>&copy; {{ "now"|date("Y") }} - Painel Administrativo</small>
        </footer>
    </div>
</body>
</html>
